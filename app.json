{
  "name": "voice-detection-api",
  "version": "1.0.0",
  "scripts": {
    "start": "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 voice_detection_api:app"
  },
  "engines": {
    "python": "3.11.x"
  }
}